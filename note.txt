First run :--  cd /Users/atharvpokale/Desktop/AdmissionPortal ur personal path
               npm install


                //is this mendetory
               "scripts": {
                 "start": "node dist/index.js",
                 "dev": "nodemon src/index.ts",
                  "build": "tsc"
                    }

                npm run dev




command to run :-- npm run dev




Before the "Admin Portal" link was added to the login page, the only way to access the admin portal was by manually typing the URL into your browser's address bar.

Here is how you would open it:

Go to your browser's address bar.
Add /admin-login to the end of your website's base URL.
For example, if you are running the project locally, the full URL would be: http://localhost:5173/admin-login





add this --->.   Add a "Sign Out" button to the bottom of the mobile menu for logged-in users.